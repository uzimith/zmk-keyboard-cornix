/*
 * Copyright (c) 2020 The ZMK Contributors
 *
 * SPDX-License-Identifier: MIT
 */

#include "zmk-helpers/helper.h"
#include "includes/cornix54.h"

// tap windows for ctl alt and gui

#define HM_TAPPING_TERM 250
#define HM_TAPPING_REPEAT 210

// quick tapping for shift

#define HM_TAPPING_TERM_FAST 200
#define HM_PRIOR_IDLE 70
#define BASE     0
#define WIN      1  // OS overlay: swaps CTRL <-> GUI on the base layer for Windows
#define SYMBOL   2
#define NUM      3
#define FUNCTION 4
#define ADJUST   5
#define NAVI     6
#define SWAP     7  // reachable only while a Windows-layer thumb CTRL is held

// https://github.com/urob/zmk-config/tree/main#timeless-homerow-mods

#define KEYS_L LT0 LT1 LT2 LT3 LT4 LT5 LM0 LM1 LM2 LM3 LM4 LM5 LB0 LB1 LB2 LB3 LB4 LB5  // left hand
#define KEYS_R RT0 RT1 RT2 RT3 RT4 RT5 RM0 RM1 RM2 RM3 RM4 RM5 RB0 RB1 RB2 RB3 RB4 RB5  // right hand
#define THUMBS LH1 LH0 RH0 RH1
#define KEYS_T LH1 LH0 RH0 RH1
#define ZMK_POINTING_DEFAULT_SCRL_VAL 15

#include <behaviors.dtsi>
#include <dt-bindings/zmk/bt.h>
#include <dt-bindings/zmk/ext_power.h>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/pointing.h>

/ {
    // Activate ADJUST layer by pressing raise and lower

    behaviors {
        // Deterministic (non-flipping) variants of &tog, so the OS layer state
        // always matches the profile that was selected.

        tog_on: toggle_layer_on {
            compatible = "zmk,behavior-toggle-layer";
            #binding-cells = <1>;
            toggle-mode = "on";
        };

        tog_off: toggle_layer_off {
            compatible = "zmk,behavior-toggle-layer";
            #binding-cells = <1>;
            toggle-mode = "off";
        };

        hm_l: homerow_mods_left {
            compatible = "zmk,behavior-hold-tap";
            #binding-cells = <2>;
            bindings = <&kp>, <&kp>;

            flavor = "balanced";
            tapping-term-ms = <HM_TAPPING_TERM>;
            quick-tap-ms = <200>;                // repeat on tap-into-hold
            require-prior-idle-ms = <HM_PRIOR_IDLE>;
            hold-trigger-key-positions = <KEYS_R KEYS_T>;
            hold-trigger-on-release;
        };

        hm_r: homerow_mods_right {
            compatible = "zmk,behavior-hold-tap";
            #binding-cells = <2>;
            bindings = <&kp>, <&kp>;

            flavor = "balanced";
            tapping-term-ms = <HM_TAPPING_TERM>;
            quick-tap-ms = <200>;
            require-prior-idle-ms = <HM_PRIOR_IDLE>;
            hold-trigger-key-positions = <KEYS_L KEYS_T>;
            hold-trigger-on-release;
        };

        // Positional Homerow mods for shift
        // Use faster tapping term and disable some features that may
        // cause false negatives.

        hm_shift_l: hm_shift_l {
            compatible = "zmk,behavior-hold-tap";
            #binding-cells = <2>;
            bindings = <&kp>, <&kp>;

            //flavor = "balanced";

            flavor = "balanced";
            tapping-term-ms = <HM_TAPPING_TERM_FAST>;
            quick-tap-ms = <200>;
            require-prior-idle-ms = <HM_PRIOR_IDLE>;
            hold-trigger-key-positions = <KEYS_R KEYS_T>;
            hold-trigger-on-release;
        };

        hm_shift_r: hm_shift_r {
            compatible = "zmk,behavior-hold-tap";
            #binding-cells = <2>;
            bindings = <&kp>, <&kp>;

            flavor = "balanced";
            tapping-term-ms = <HM_TAPPING_TERM_FAST>;
            quick-tap-ms = <200>;
            require-prior-idle-ms = <HM_PRIOR_IDLE>;
            hold-trigger-key-positions = <KEYS_L KEYS_T>;

            // for quick tapping; shift+~

            hold-trigger-on-release;
        };

        /*
         * Non-Positional Homerow Mods
         * Used for &mm_grescm_gui behavior on left hand.
         *
         * Usage: &hm LSHFT T
         * Tap: T
         * Tap-Tap-Hold: Repeat T
         * Hold: LSHIFT
         */

        hm: homerow_mods {
            compatible = "zmk,behavior-hold-tap";
            #binding-cells = <2>;
            bindings = <&kp>, <&kp>;

            flavor = "balanced";
            tapping-term-ms = <HM_TAPPING_TERM>;
            quick-tap-ms = <200>;
        };

        hm_lt: homerow_mods_layer_tap {
            compatible = "zmk,behavior-hold-tap";
            #binding-cells = <2>;
            bindings = <&mo>, <&kp>;

            flavor = "balanced";
            tapping-term-ms = <HM_TAPPING_TERM>;
            quick-tap-ms = <200>;
        };

        mouse_scrl: mouse_wheel_scrl {
            compatible = "zmk,behavior-sensor-rotate-var";
            #sensor-binding-cells = <2>;
            bindings = <&msc>, <&msc>;

            tap-ms = <10>;
        };

        lt: layer_tap {
            compatible = "zmk,behavior-hold-tap";
            #binding-cells = <2>;
            bindings = <&mo>, <&kp>;

            flavor = "hold-preferred";
            tapping-term-ms = <HM_TAPPING_TERM>;
            quick-tap-ms = <200>;
        };

        lt_mkp: lt_mkp {
            compatible = "zmk,behavior-hold-tap";
            label = "LT_MKP";
            bindings = <&mo>, <&mkp>;

            #binding-cells = <2>;
            tapping-term-ms = <200>;
        };

        mod_mkp: mod_mkp {
            compatible = "zmk,behavior-hold-tap";
            label = "MOD_MKP";
            bindings = <&kp>, <&mkp>;

            #binding-cells = <2>;
            tapping-term-ms = <200>;
        };

        /*
         * TAB on the SWAP layer. The first press, with CTRL still held, runs
         * &alt_tab, which drops CTRL from the report and holds ALT down. That
         * clears MOD_LCTL/MOD_RCTL from the explicit modifiers the morph
         * tests, so every later press takes the plain &kp TAB branch and
         * lands as ALT+TAB with the ALT that is still held. SHIFT is not
         * listed in mods, so CTRL+SHIFT+TAB cycles backwards.
         */

        ctl_tab: ctrl_tab_as_alt_tab {
            compatible = "zmk,behavior-mod-morph";
            #binding-cells = <0>;
            bindings = <&kp TAB>, <&alt_tab>;
            mods = <(MOD_LCTL|MOD_RCTL)>;
        };
    };

    /*
     * Bluetooth profile switching that also selects the OS modifier layout.
     * &bt_mac N -> select profile N and use the Mac layout (GUI = Command)
     * &bt_win N -> select profile N and use the Windows layout (CTRL swapped in)
     */

    macros {
        bt_mac: bt_profile_mac {
            compatible = "zmk,behavior-macro-one-param";
            #binding-cells = <1>;
            label = "BT_MAC";
            bindings = <&macro_param_1to2 &bt BT_SEL MACRO_PLACEHOLDER &tog_off WIN>;
        };

        bt_win: bt_profile_win {
            compatible = "zmk,behavior-macro-one-param";
            #binding-cells = <1>;
            label = "BT_WIN";
            bindings = <&macro_param_1to2 &bt BT_SEL MACRO_PLACEHOLDER &tog_on WIN>;
        };

        /*
         * Hand off from CTRL to a held ALT for app switching. Releasing a
         * modifier that is not held is a guarded no-op in ZMK (it logs and
         * returns), so the unbalanced releases here are safe: &alt_tab
         * releases whichever CTRL was held, and win_lctl/win_rctl release an
         * ALT that only exists if &alt_tab ran.
         */

        alt_tab: alt_tab_first_press {
            compatible = "zmk,behavior-macro";
            #binding-cells = <0>;
            label = "ALT_TAB";
            wait-ms = <15>;
            tap-ms = <15>;
            bindings = <&macro_release &kp LCTRL &kp RCTRL>, <&macro_press &kp LALT>, <&macro_tap &kp TAB>;
        };

        /*
         * CTRL on the Windows layer thumbs. Holding one also holds the SWAP
         * layer, which is the only place &ctl_tab lives, so ALT can never be
         * pressed without this key's release being there to let it go. That
         * release is what commits the app switcher.
         */

        win_lctl: win_left_ctrl {
            compatible = "zmk,behavior-macro";
            #binding-cells = <0>;
            label = "WIN_LCTL";
            wait-ms = <0>;
            bindings = <&macro_press &kp LCTRL &mo SWAP>, <&macro_pause_for_release>, <&macro_release &kp LCTRL &kp LALT &mo SWAP>;
        };

        win_rctl: win_right_ctrl {
            compatible = "zmk,behavior-macro";
            #binding-cells = <0>;
            label = "WIN_RCTL";
            wait-ms = <0>;
            bindings = <&macro_press &kp RCTRL &mo SWAP>, <&macro_pause_for_release>, <&macro_release &kp RCTRL &kp LALT &mo SWAP>;
        };
    };

    conditional_layers {
        compatible = "zmk,conditional-layers";

        function_layer {
            if-layers = <3 2>;
            then-layer = <4>;
        };
    };

    combos {
        compatible = "zmk,combos";

        left_brace {
            bindings = <&kp LEFT_BRACE>;
            timeout-ms = <25>;
            key-positions = <4 3>;
            layers = <0>;
        };

        right_brace {
            bindings = <&kp RIGHT_BRACE>;
            timeout-ms = <25>;
            key-positions = <7 8>;
            layers = <0>;
        };

        left_parenthesis {
            bindings = <&kp LEFT_PARENTHESIS>;
            timeout-ms = <25>;
            key-positions = <16 17>;
            layers = <0>;
        };

        right_parenthesis {
            bindings = <&kp RIGHT_PARENTHESIS>;
            timeout-ms = <25>;
            key-positions = <19 18>;
            layers = <0>;
        };

        left_bracket {
            bindings = <&kp LEFT_BRACKET>;
            timeout-ms = <25>;
            key-positions = <28 27>;
            layers = <0>;
        };

        right_bracket {
            bindings = <&kp RIGHT_BRACKET>;
            timeout-ms = <20>;
            key-positions = <33 34>;
            layers = <0>;
        };

        click {
            bindings = <&mkp MB3>;
            timeout-ms = <30>;
            key-positions = <15 16>;
            layers = <6>;
        };

        esc_2 {
            bindings = <&kp ESCAPE>;
            key-positions = <1 2>;
            timeout-ms = <30>;
            layers = <0>;
        };

        tab {
            bindings = <&kp TAB>;
            key-positions = <2 3>;
            timeout-ms = <30>;
            layers = <0>;
        };

        middleclick {
            bindings = <&mkp MB3>;
            key-positions = <34 33>;
            layers = <6>;
            timeout-ms = <30>;
        };
    };

    keymap {
        compatible = "zmk,keymap";

        default_layer {
            display-name = "Base";

            // ------------------------------------------------------------------------------------------------------------
            // |   `   |  1  |  2  |  3   |  4   |  5   |                   |  6   |  7    |  8    |  9   |   0   |       |
            // |  ESC  |  Q  |  W  |  E   |  R   |  T   |                   |  Y   |  U    |  I    |  O   |   P   | BKSPC |
            // |  TAB  |  A  |  S  |  D   |  F   |  G   |                   |  H   |  J    |  K    |  L   |   ;   |   '   |
            // | SHIFT |  Z  |  X  |  C   |  V   |  B   |  MUTE  |  |       |  N   |  M    |  ,    |  .   |   /   | SHIFT |
            //               | GUI | ALT  | CTRL | LOWER|  ENTER |  | SPACE | RAISE| CTRL  | ALT   | GUI  |

            bindings = <
&kp TAB  &kp Q          &kp W             &kp E                 &kp R                &kp T                     &kp Y  &kp U                &kp I                  &kp O              &kp P             &kp BACKSPACE
&none    &hm_l LCTRL A  &hm_l LALT S      &hm_l LEFT_COMMAND D  &hm_shift_l LSHFT F  &kp G                     &kp H  &hm_shift_r RSHFT J  &hm_r RIGHT_COMMAND K  &hm_r RALT L       &hm_r RCTRL SEMI  &kp ENTER
&none    &kp Z          &kp X             &kp C                 &kp V                &kp B  &kp C_MUTE  &none  &kp N  &kp M                &kp COMMA              &lt 6 PERIOD       &kp SLASH         &kp MINUS
&trans   &kp LEFT_ALT   &kp LEFT_COMMAND  &lt 6 LANG2           &lt 2 SPACE          &mo 3                     &mo 6  &lt 2 SPACE          &lt 3 LANG1            &kp RIGHT_COMMAND  &kp RIGHT_ALT     &mo 5
            >;

            sensor-bindings =
                <&inc_dec_kp C_VOL_UP C_VOL_DN>,
                <&inc_dec_kp C_BRI_DEC C_BRI_UP>;
        };

        win_layer {
            display-name = "Win";

            /*
             * OS overlay for Windows hosts. Everything is &trans and falls
             * through to the base layer; only the CTRL/GUI positions are
             * overridden so that the finger holding Command on macOS holds
             * Control on Windows (and vice versa).
             *
             * This layer sits directly above BASE so every momentary layer
             * (SYMBOL and above) still takes precedence over it.
             *
             * The thumb CTRLs are macros: they hold CTRL and the SWAP layer
             * together, so CTRL+TAB switches apps the way CMD+TAB does on
             * macOS, with ALT held until the thumb comes up. The Navi layer
             * keeps a plain &kp LC(TAB) for cycling browser tabs.
             */

            bindings = <
&trans  &trans  &trans     &trans  &trans  &trans                  &trans  &trans  &trans  &trans     &trans  &trans
&trans  &trans  &trans     &trans  &trans  &trans                  &trans  &trans  &trans  &trans     &trans  &trans
&trans  &trans  &trans     &trans  &trans  &trans  &trans  &trans  &trans  &trans  &trans  &trans     &trans  &trans
&trans  &trans  &win_lctl  &trans  &trans  &trans                  &trans  &trans  &trans  &win_rctl  &trans  &trans
            >;
        };

        Symbol_layer {
            display-name = "Symbols";

            // TODO: Some binds are waiting for shifted keycode support.
            // -------------------------------
            // |       |  F1 |  F2 |  F3  |  F4  |  F5  |                   |  F6  |  F7   |  F8   |  F9  |  F10  |  F11  |
            // |   `   |  1  |  2  |  3   |  4   |  5   |                   |  6   |  7    |  8    |  9   |   0   |  F12  |
            // |       |  !  |  @  |  #   |  $   |  %   |                   |  ^   |  &    |  *    |  (   |   )   |   |   |
            // |       |  =  |  -  |  +   |  {   |  }   |        |  |       |  [   |  ]    |  ;    |  :   |   \   |       |
            //               |     |      |      |      |        |  |       |      |       |       |      |

            bindings = <
&none  &kp EXCL           &kp AT    &kp HASH          &kp DLLR                  &kp PRCNT                  &kp CARET          &kp AMPS                  &kp ASTRK                 &kp LPAR              &kp RPAR         &none
&none  &hm_l LCTRL GRAVE  &kp LALT  &kp LEFT_COMMAND  &hm_shift_l LSHIFT MINUS  &kp PLUS                   &kp EQUAL          &hm_shift_l RSHIFT TILDE  &hm_r RIGHT_COMMAND PIPE  &hm_r RALT BACKSLASH  &hm_r RCTRL SQT  &none
&none  &none              &none     &none             &none                     &none      &trans  &trans  &kp DOUBLE_QUOTES  &kp SQT                   &none                     &none                 &none            &none
&none  &none              &none     &trans            &trans                    &trans                     &trans             &trans                    &kp N1                    &none                 &none            &none
            >;
        };

        Num_layer {
            display-name = "Mix";

            // ------------------------------------------------------------------------------------------------------------
            // | BTCLR | BT1  | BT2  |  BT3  |  BT4  |  BT5 |                |      |      |       |      |       |       |
            // |       | INS  | PSCR | GUI   |       |      |                | PGUP |      |   ^   |      |       |       |
            // |       | ALT  | CTRL | SHIFT |       | CAPS |                | PGDN |   <- |   v   |  ->  |  DEL  | BKSPC |
            // |       | UNDO | CUT  | COPY  | PASTE |      |      |  |      |      |      |       |      |       |       |
            //                |      |       |       |      |      |  |      |      |      |       |      |

            bindings = <
&trans  &trans          &trans         &trans                 &trans                 &trans                    &none      &none                  &kp ASTERISK            &none          &none           &trans
&trans  &hm_l LCTRL N1  &hm_l LALT N2  &hm_l LEFT_COMMAND N3  &hm_shift_l LSHIFT N4  &kp N5                    &kp N6     &hm_shift_r RSHIFT N7  &hm_r RIGHT_COMMAND N8  &hm_r RALT N9  &hm_r RCTRL N0  &trans
&trans  &trans          &trans         &trans                 &kp MINUS              &kp PLUS  &trans  &trans  &kp EQUAL  &trans                 &trans                  &trans         &trans          &trans
&none   &none           &trans         &trans                 &trans                 &trans                    &trans     &trans                 &kp N2                  &none          &none           &none
            >;

            label = "Num";
        };

        Function_layer {
            bindings = <
&trans  &kp F1  &kp F2  &kp F3                          &kp F4                         &kp F5                           &kp F6      &kp F7                   &kp F8                     &kp F9  &kp F10  &kp F11
&trans  &trans  &trans  &hm_l LEFT_COMMAND PRINTSCREEN  &hm_shift_l LSHIFT SCROLLLOCK  &kp PAUSE_BREAK                  &kp INSERT  &hm_shift_l RSHIFT HOME  &hm_r RIGHT_COMMAND PG_UP  &trans  &trans   &kp F12
&trans  &trans  &trans  &trans                          &trans                         &trans           &trans  &trans  &kp DEL     &kp END                  &kp PAGE_DOWN              &trans  &trans   &trans
&trans  &trans  &trans  &trans                          &trans                         &trans                           &trans      &trans                   &kp N3                     &trans  &trans   &trans
            >;

            label = "Function";
        };

        Adjust_layer {
            display-name = "Adjust";

            // ----------------------------------------------------------------------------------------------------------------------------
            // | BTCLR  |  BT1    |  BT2    |   BT3   |   BT4   |   BT5   |                  |      |      |       |      |       |       |
            // | moDEBUG| RGB_HUD | RGB_HUI | RGB_SAD | RGB_SAI | RGB_EFF |                  |      |      |       |      |       |       |
            // | EXTPWR | RGB_BRD | RGB_BRI |         |         |         |                  |      |      |       |      |       |       |
            // |        |         |         |         |         |         | RGB_TOG | |      |      |      |       |      |       |       |
            //                    |         |         |         |         |         | |      |      |      |       |      |
            //
            // Profile 0 also selects the Mac layout, profile 1 the Windows
            // layout. &tog WIN (bottom row, C column) flips the OS layout by
            // hand, which is what the dongle build needs since it never
            // switches profiles.

            bindings = <
&bt BT_CLR      &bt_mac 0      &bt_win 1      &bt BT_SEL 2   &bt BT_SEL 3   &bt BT_SEL 4                   &none  &none  &none   &none  &none             &none
&kp CAPSLOCK    &bt BT_DISC 0  &bt BT_DISC 1  &bt BT_DISC 2  &bt BT_DISC 3  &bt BT_DISC 4                  &none  &none  &none   &none  &none             &none
&bt BT_CLR_ALL  &none          &none          &tog WIN       &none          &none          &trans  &trans  &none  &none  &none   &none  &ext_power EP_ON  &none
&none           &none          &none          &none          &none          &none                          &none  &none  &kp N4  &none  &none             &none
            >;
        };

        Navi_layer {
            display-name = "Navi";
            bindings = <
&none  &kp LC(LS(TAB))  &kp LC(TAB)  &kp BACKSPACE  &kp DELETE  &none                   &trans    &none     &none     &none      &none      &none
&none  &trans           &trans       &mkp MB2       &mkp MB1    &trans                  &kp LEFT  &kp DOWN  &kp UP    &kp RIGHT  &kp RCTRL  &none
&none  &none            &none        &none          &none       &trans  &trans  &trans  &trans    &mkp MB1  &mkp MB2  &none      &none      &none
&none  &none            &none        &trans         &trans      &trans                  &trans    &trans    &kp N5    &none      &none      &none
            >;
        };

        swap_layer {
            display-name = "Swap";

            /*
             * Held by the Windows layer thumb CTRLs and nothing else, which
             * is what keeps a held ALT from outliving the key that started
             * it. Only TAB is bound; the rest falls through, so CTRL+C and
             * friends still work while the thumb is down.
             */

            bindings = <
&ctl_tab  &trans  &trans  &trans  &trans  &trans                  &trans  &trans  &trans  &trans  &trans  &trans
&trans    &trans  &trans  &trans  &trans  &trans                  &trans  &trans  &trans  &trans  &trans  &trans
&trans    &trans  &trans  &trans  &trans  &trans  &trans  &trans  &trans  &trans  &trans  &trans  &trans  &trans
&trans    &trans  &trans  &trans  &trans  &trans                  &trans  &trans  &trans  &trans  &trans  &trans
            >;
        };
    };
};
